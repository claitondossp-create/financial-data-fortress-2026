# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Ambientes Virtuais
venv/
env/
ENV/
.env
.venv
pip-log.txt
pip-delete-this-directory.txt

# Jupyter Notebooks
.ipynb_checkpoints
*/.ipynb_checkpoints/*

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db
Desktop.ini
.DS_Store

# =========================
# PROJETO FINANCEIRO
# =========================

# Segurança (CRÍTICO: NUNCA COMMITAR CHAVES!)
security/*.key
security/master.key
security/salt.key

# Logs de Auditoria e Temporários
audit_logs/*.json
logs/
*.log

# Outputs Gerados (Manter apenas o código que gera, não os dados)
# Se quiser manter exemplos, use git add -f
outputs/alerts/*.json
outputs/quarantine/*.csv
outputs/reports/*.md

# Metadados de Controle (CDC)
metadata/*.db
metadata/*.sqlite

# Dados (Geralmente não versionamos dados grandes, mas como é um projeto demo,
# manteremos os CSVs de exemplo se forem pequenos. Se forem grandes (>100MB), ignorar)
# data/01_bronze/*.csv
# data/02_silver/*.csv
# data/03_gold/*.csv

# Cache do Power BI Desktop (gerado automaticamente — não versionar)
**/.pbi/cache.abf
